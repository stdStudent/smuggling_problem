#ifndef SMUGGLING_PROBLEM_SMUGGLE_H
#define SMUGGLING_PROBLEM_SMUGGLE_H

#include <vector>
#include <climits>
#include <span>

using std::vector;

vector<int> smuggle(vector<int>& a);

#endif //SMUGGLING_PROBLEM_SMUGGLE_H
